# pplx-proxy system prompt whitelist
# Only lines matching at least one pattern are KEPT. Everything else stripped.
# One regex per line. # comments and empty lines ignored.
# (?i) MUST be at start of pattern for case-insensitive matching.

# Language preference (exclude lines with XML tags via negative lookahead)
(?i)^(?!.*<).*(traditional chinese|繁體|zh-tw|reply in|respond in|回覆|回答.*語言|same language)
(?i)^conversation language:

# Output format rules (exclude XML)
(?i)^(?!.*<).*(禁止輸出|絕對禁止|不要輸出|do not output|never output)
(?i)^(?!.*<).*(latex|katex|數學公式|math formula)
